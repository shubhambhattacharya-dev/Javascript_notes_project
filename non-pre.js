//object

// SYNTAX:

let obj={};// its a valid object

console.log(obj);
console.log(typeof(obj));//object

// for better we use an example
//user loggedin  : alway use key value pair
let userName={
       firstName:"shubham Bhattacharya",
       loggedIn:true               // imp: TO ASSIGN VALUE USE " : "(USE COLON) AND FOR SEPARATION USE " ,"(COMMA)
};

console.log(userName);