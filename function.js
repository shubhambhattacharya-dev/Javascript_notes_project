//
function sum(a,b) {// the thread reach here and check oh! its a function. and run i t yet just store   
    const sumValue=(a+b);//third here check operation
    return sumValue;//here return value store in value variable last 
}

const value=sum(4,6);// second thread reach here sum is call
const value2=sum(97,62);

console .log(value);// here print it in console
console .log(value2);